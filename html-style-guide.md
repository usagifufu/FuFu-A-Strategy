# FuFu HTML 暗色主题设计规范

所有 FuFu 报告使用统一暗色主题。可直接复制 `assets/fufu-template.html` 作为骨架，按报告类型增删区块。

## 设计令牌

| 项 | 值 |
|----|----|
| 背景 | `#0d0f14`（深黑蓝） |
| 卡片背景 | `#161a22` |
| 主色（FuFu 红） | `#e94560` |
| 主色暗 | `#b32d44` |
| 涨（红） | `#ff4d4f` / `#e94560` |
| 跌（绿） | `#2ecc71` / `#1abc9c` |
| 正文文字 | `#e6e8ec` |
| 次要文字 | `#9aa0aa` |
| 边框 | `#232833` |
| 左侧竖条 | `4px solid #e94560` |
| 免责声明 | `#6b7280`（小字灰） |

## 字体
```
font-family: -apple-system, 'PingFang SC', 'Microsoft YaHei', sans-serif;
```

## 结构约定
- 顶部标题区：🦊 FuFu <报告名> + 日期，主色强调
- 每个区块：左侧红色竖条 (`border-left`) + 区块标题（主色）+ 内容
- 关键数据（涨跌幅/仓位/止损）：`<span class="hl">` 红色高亮
- 涨用 `.up`（红），跌用 `.down`（绿）
- 买卖建议：`.signal-buy`(🟢绿底) / `.signal-hold`(🟡黄底) / `.signal-sell`(🔴红底) 醒目卡片
- 表格：隔行底色、表头主色、数字右对齐

## 信号卡片样式
- 🟢 买入：背景 `rgba(46,204,113,.15)`，边框 `#2ecc71`，文字 `#2ecc71`
- 🟡 持有观望：背景 `rgba(241,196,15,.15)`，边框 `#f1c40f`，文字 `#f1c40f`
- 🔴 卖出：背景 `rgba(233,69,96,.15)`，边框 `#e94560`，文字 `#e94560`

## 约束
- 全部内联 style，**无外部依赖**（不引 CDN/字体文件）
- 自包含、可直接浏览器打开
- 底部固定免责声明："本报告由 FuFu（AI 半自主交易员）基于公开数据自动生成，仅供研究参考，不构成投资建议。市场有风险，投资需谨慎。"

## 红涨绿跌强制
A股惯例：上涨用红色、下跌用绿色。凡展示涨跌幅、K线、信号颜色，均遵守此规则，不得反置。
